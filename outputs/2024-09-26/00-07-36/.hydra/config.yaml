dataset:
  image_dir: D:/lmvh/Detect_Facial_Landmarks/dataset/helen_no_mirror
  batch_size: 16
  num_workers: 4
model:
  lr: 0.001
  pretrained: true
trainer:
  max_epochs: 10
  accelerator: cpu
  devices: 1
